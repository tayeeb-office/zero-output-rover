<?php
/**
 * ZERO OUTPUT — Page Footer
 * Per design-reference/mission-control, the mission-control layout is a
 * full-height sidebar + header + main-content view with no page footer on
 * any screen. Intentionally left without markup — the sidebar's own status
 * block (sidebar__footer) covers connection/firmware info instead.
 */
